<!DOCTYPE html>
<html>
<head>
  <title>Conta Certa</title>
  <meta name="google-site-verification" content="MCE0X80bA0McyIw4jdq0HmNBDNvx7CpnGKFL0N3dE1k" />
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/flatpickr/4.6.13/flatpickr.min.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/flatpickr/4.6.13/themes/material_blue.css">
  <link href='https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css' rel='stylesheet'>
  <%= favicon_link_tag "logo/logo.toggle.png", type: "image/png" %>
  <meta property="og:title" content="Conta Certa" />
  <meta property="og:description" content="Transparência nas Despesas, Harmonia nas Amizades." />
  <meta property="og:image" content="<%= image_url 'logo/logo.toggle.png' %>" />
  <meta property="og:url" content="<%= request.original_url %>" />
  <meta property="og:type" content="website" />
  <meta name="twitter:card" content="summary_large_image" />
  <meta name="twitter:title" content="Conta Certa" />
  <meta name="twitter:description" content="Transparência nas Despesas, Harmonia nas Amizades." />
  <meta name="twitter:image" content="<%= image_url 'logo/logo.toggle.png' %>" />
  <meta name="twitter:url" content="<%= request.original_url %>" />
</head>
<body>
  <%= render "shared/navbar" %>
  <div class="container">
    <div class="row justify-content-center">
      <% if controller.controller_name == "groups" && controller.action_name == "show" %>
        <!-- Aplica classes específicas para a página de groups -->
        <div class="col-sm-12 col-md-10 col-lg-8 col-xl-7 col-xxl-6 modal-index-wrapper">
      <% else %>
        <!-- Aplica classes padrão para outras páginas -->
        <div class="col-sm-10 col-md-8 col-lg-6 col-xl-5 col-xxl-4 modal-index-wrapper">
      <% end %>

      <% if notice.present? %>
        <div class="notice">
          <%= notice %>
        </div>
      <% end %>

     <% if flash[:alertgroup] %>
        <div class="alert-token">
          <%= flash[:alertgroup] %>
        </div>
      <% end %>

      <% if flash[:token].present? %>
        <div class="alert-token">
          <%= flash[:token] %>
        </div>
      <% end %>
      <!-- Outros conteúdos -->
      </div>
    </div>
  </div>

  <%= yield %>
</body>
</html>
